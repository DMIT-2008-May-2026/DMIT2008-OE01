## Pokeman Twitch!

Making our own html elements `HTMLelement`\
Essentially digital legos from HTML

- Separate file for each component
- Callback functions don't fire right away
- `name`, class, . is a class
- `# means privately scoped`
- When to grab from the document? you can use this.something instead

`connectedCallback()` What is written here loads into the DOM
`observedAttributes()`
`attributeChangedCallback`

### Break everything down into individual components
> 1. Components into a folder (the container itself is often a component)
> 2. Make an empty function for what component you want (eg;expenseform) `skelly`
> 3. `<expense-form></expense-form>` can be added into the HTML now
> 4. 



this.something

### Render
- Render methods should be separate logic